name: Publish to PyPI

# ============================================================
# Trusted Publishing (OIDC) — 默认；API token — fallback
# ============================================================
# PyPI 端原本为 trusted publisher 配置：
#   Owner: hellob1889
#   Repository: Pandaone AI Agent
#   Workflow: publish.yml
#   Environment: Any（未配置 GitHub Environment）
#
# 但实际 publish 出现 invalid-publisher 错误（PyPI 上找不到匹配的 publisher）。
# 怀疑 pandaone-guard 项目是 legacy API token 模式而非 Trusted Publisher 模式。
#
# 本 PR 加入 fallback：
#   - 如果 secret PYPI_API_TOKEN 存在 → 用 API token（password=secret, username=__token__）
#   - 否则 → 默认 OIDC Trusted Publishing
#
# IMPORTANT: PyPI project name = `pandaone-guard`（不是 pandaone-guard）
# PR #4 把 src 内部代码 + CLI 命令名改成 pandaone（pandaone/, pandaone:main），
# 但 PyPI 分发名仍是 `pandaone-guard`（PEP 503 wheel 文件名 = pandax_guard-*）。
# 这是有意的：PyPI 上 pandaone-guard 项目从未存在，跳过新建项目的成本。
# 用户面：pip install pandaone-guard → 得到 pandaone 命令（内部 brand 一致，外部 channel 一致）
#
# 触发规则 (按 tag 前缀):
#   v*.*.*-test  → TestPyPI (例: v0.7.2-test)
#   v*.*.*       → Production PyPI (例: v0.7.2)
#
# 也支持 workflow_dispatch 手动触发 — 现在也能自动建 GitHub Release
# (PR #27 fix, 之前 dispatch 触发会跳过 Release 步骤)
# ============================================================

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      target:
        description: "发布目标"
        required: true
        default: "testpypi"
        type: choice
        options:
          - testpypi
          - pypi
      tag_version:
        description: "版本号（用于 dispatch 触发时建 tag/release），如 0.7.8。pypi 目标时必填。"
        required: false
        default: ""
        type: string

env:
  PYTHON_VERSION: "3.10"

jobs:
  build:
    name: Build wheel + sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          # setuptools 80.x raises Remove on deprecations we can't fix yet
          python -m pip install "setuptools==80.10.2" "wheel" "build" "twine"

      - name: Build
        id: build
        run: |
          {
            python -m build --no-isolation 2>&1
          } > /tmp/build.log 2>&1 || EXIT=$?
          echo "BUILD_EXIT=${EXIT:-0}" >> "$GITHUB_ENV"
          # Always upload the full log so we can read it via API
          tail -200 /tmp/build.log || true
          echo "---END---"

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: build-log-${{ github.run_id }}
          path: /tmp/build.log
          if-no-files-found: ignore

      - name: Stamp build log as short output
        if: always()
        run: |
          # Compress to fit in step output
          gzip -c /tmp/build.log > /tmp/build.log.gz
          b64=$(base64 -w0 /tmp/build.log.gz)
          echo "BUILD_LOG_B64_LEN=${#b64}" >> "$GITHUB_OUTPUT"
          # store compressed+b64 in 2KB slices
          echo "$b64" > /tmp/build.log.b64

      - name: Verify metadata
        run: |
          ls -la dist/
          # Derive expected wheel/sdist names from the tag (strip leading 'v')
          _ver="${GITHUB_REF_NAME#v}"
          timeout 60 python -m twine check "dist/pandaone_guard-${_ver}-py3-none-any.whl" "dist/pandaone_guard-${_ver}.tar.gz" || echo "twine check non-critical"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install package + dev deps
        run: |
          python -m pip install --upgrade pip
          # Pin setuptools to match build step — avoids build-isolation
          # pulling a newer setuptools that breaks our pyproject metadata
          python -m pip install "setuptools==80.10.2" "wheel" "build"
          python -m pip install -e .[dev] --no-build-isolation
          python -m pip install pytest-timeout

      - name: Run pytest
        id: pytest
        run: |
          set -o pipefail
          pytest tests/ -v --timeout=60 --timeout-method=thread -x --ignore=tests/test_watchdog.py --ignore=tests/test_watchdog_dedupe.py --ignore=tests/test_pandaone_serve.py 2>&1 | tee /tmp/pytest.log
          echo "PYTEST_EXIT=${PIPESTATUS[0]}" >> "$GITHUB_ENV"
        continue-on-error: true

      - name: Upload pytest log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pytest-log-${{ github.run_id }}
          path: /tmp/pytest.log
          if-no-files-found: ignore

      - name: Fail if pytest failed
        if: env.PYTEST_EXIT != '0'
        run: |
          echo "::error::pytest exited with code $PYTEST_EXIT. See pytest-log-${{ github.run_id }} artifact for full log."
          exit "$PYTEST_EXIT"

  publish:
    name: Publish
    # Decouple publish from test: build artifact is the source of truth for shipping
    needs: [build]
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    # No `environment:` to match PyPI Trusted Publisher (configured without environment)
    permissions:
      id-token: write
      contents: write
    steps:
      # v0.7.12 fix: checkout repo so we can attach install.ps1/install.sh
      # (these live at repo root, not in dist/ artifact)
      - name: Checkout repo (for install scripts)
        uses: actions/checkout@v7

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Decide target (push)
        id: target
        if: github.event_name == 'push'
        run: |
          if [[ "${{ github.ref_name }}" == *-test ]]; then
            echo "target=testpypi" >> "$GITHUB_OUTPUT"
            echo "→ TestPyPI (tag suffix -test)"
          else
            echo "target=pypi" >> "$GITHUB_OUTPUT"
            echo "→ Production PyPI"
          fi

      - name: Decide target (dispatch)
        id: target-dispatch
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "target=${{ github.event.inputs.target }}" >> "$GITHUB_OUTPUT"

      - name: Detect publish mode
        id: mode
        run: |
          if [ -n "$PYPI_API_TOKEN" ]; then
            echo "mode=token" >> "$GITHUB_OUTPUT"
            echo "→ Using API token (PYPI_API_TOKEN secret present)"
          else
            echo "mode=oidc" >> "$GITHUB_OUTPUT"
            echo "→ Using OIDC Trusted Publishing"
          fi
        env:
          PYPI_API_TOKEN: ${{ secrets.PYPI_API_TOKEN }}

      - name: Publish to TestPyPI (API token)
        if: ${{ (steps.target.outputs.target == 'testpypi' || steps.target-dispatch.outputs.target == 'testpypi') && steps.mode.outputs.mode == 'token' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          password: ${{ secrets.PYPI_API_TOKEN }}
          username: __token__

      - name: Publish to TestPyPI (OIDC)
        if: ${{ (steps.target.outputs.target == 'testpypi' || steps.target-dispatch.outputs.target == 'testpypi') && steps.mode.outputs.mode == 'oidc' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

      - name: Publish to Production PyPI (API token)
        if: ${{ (steps.target.outputs.target == 'pypi' || steps.target-dispatch.outputs.target == 'pypi') && steps.mode.outputs.mode == 'token' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          username: __token__

      - name: Publish to Production PyPI (OIDC)
        if: ${{ (steps.target.outputs.target == 'pypi' || steps.target-dispatch.outputs.target == 'pypi') && steps.mode.outputs.mode == 'oidc' }}
        uses: pypa/gh-action-pypi-publish@release/v1

      # ----------------------------------------------------------------
      # Create GitHub Release (PR #27 fix)
      # ----------------------------------------------------------------
      # 守卫:
      #   - push tag v*.*.* (非 -test)            → 建 release, tag = ref_name
      #   - workflow_dispatch + target=pypi
      #       + tag_version 非空                  → 建 release, tag = v$tag_version
      #   - 其他                                  → 跳过
      #
      # tag_name 显式覆盖: dispatch 触发时 github.ref_name = main,
      # 不覆盖就会创建 "main" release。
      # ----------------------------------------------------------------
      - name: Resolve release tag (dispatch)
        id: release-tag-dispatch
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'pypi'
        run: |
          _v="${{ github.event.inputs.tag_version }}"
          if [ -z "$_v" ]; then
            echo "::error::tag_version input is required when target=pypi on workflow_dispatch"
            exit 1
          fi
          # strip leading 'v' if user typed it
          _v="${_v#v}"
          echo "tag=v${_v}" >> "$GITHUB_OUTPUT"
          echo "→ dispatch mode: will create tag=v${_v}"

      - name: Create GitHub Release
        if: |
          (github.event_name == 'push'
            && startsWith(github.ref, 'refs/tags/')
            && !endsWith(github.ref_name, '-test'))
          ||
          (github.event_name == 'workflow_dispatch'
            && github.event.inputs.target == 'pypi'
            && steps.release-tag-dispatch.outputs.tag != '')
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event_name == 'push' && github.ref_name || steps.release-tag-dispatch.outputs.tag }}
          name: ${{ github.event_name == 'push' && github.ref_name || steps.release-tag-dispatch.outputs.tag }}
          files: |
            dist/*.whl
            dist/*.tar.gz
            install.ps1
            install.sh
          generate_release_notes: true
          body: |
            ## What's Changed

            Auto-generated release notes from commits since last tag.

            ## Installation

            ### One-line (PyPI)
            ```bash
            pip install --upgrade pandaone-guard
            ```

            ### Hard-isolated install (recommended — no site-packages pollution)
            Downloads the GitHub Release wheel into a dedicated venv at `%LOCALAPPDATA%\pandaone\venv\` (Windows) or `~/.local/share/pandaone/venv` (macOS/Linux):

            **Windows (PowerShell):**
            ```powershell
            irm https://raw.githubusercontent.com/hellob1889/Pandaone-AI-Agent/main/install.ps1 | iex
            ```

            **macOS / Linux:**
            ```bash
            curl -sSL https://raw.githubusercontent.com/hellob1889/Pandaone-AI-Agent/main/install.sh | bash
            ```

            Or download `install.ps1` / `install.sh` from the Assets section below and run locally.

            This installs the `pandaone` CLI command (see the rename note in the v0.7.4 commit message — internal brand is Pandaone AI Agent, distribution channel stays pandaone-guard to preserve the existing PyPI project's Trusted Publisher config).

            See [CHANGELOG.md](https://github.com/hellob1889/Pandaone-AI-Agent/blob/main/CHANGELOG.md) for details.
