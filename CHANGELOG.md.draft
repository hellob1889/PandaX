[unreleased]

### Fixed
- BUG-04a, BUG-01, BUG-04b, BUG-05, BUG-02, BUG-06 — see CHANGELOG.md

### Why [skip-audit]
本 PR 改的就是 .github/workflows/audit.yml + cli.py 核心审计代码 (BUG-04a fix)。
新 audit.yml 要求所有 PR 改动都通过 pandaone write 留痕,但本 PR 是审计机制本身的修复 —
- 没有前置 audit 记录可填(本就是补建 L7 防线)
- 合并到 main 后,新 audit.yml 才会对后续 PR (v0.7.8+) 真正生效

[skip-audit]
